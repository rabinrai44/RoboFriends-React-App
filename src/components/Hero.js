import React from 'react';

const Hero = () => {
    return (
        <div className="container">
            <h1 className="jumbotron-heading">
                Find your's Robot Friends
            </h1>
        </div>
    );
}

export default Hero;